(()=>{
  const link=document.querySelector('#sellerProfileLink'),modal=document.querySelector('#sellerProfileModal'),card=modal?.querySelector('.seller-profile-card');if(!link||!card)return;
  link.addEventListener('click',async event=>{event.preventDefault();event.stopImmediatePropagation();const car=window.vklucheCurrentCar?.(),client=window.vklucheAuth?.getClient?.();if(!car)return;modal.classList.add('open');card.innerHTML='<button class="modal-close" type="button" data-public-seller-close>×</button><div class="seller-profile-loading">Загружаем профиль продавца…</div>';let profile=null;if(client&&car.listingId){const{data}=await client.rpc('public_seller_profile',{p_listing_id:car.listingId});profile=data}profile=profile||{name:car.details?.seller||'Продавец',listingCount:1,dealCount:0,rating:0,reviewCount:0,branches:[],reviews:[]};const initial=String(profile.name||'П').charAt(0).toUpperCase(),contact=[profile.phone&&`<a href="tel:${profile.phone}">${profile.phone}</a>`,profile.email&&`<a href="mailto:${profile.email}">${profile.email}</a>`,profile.website&&`<a href="${profile.website}" target="_blank" rel="noopener">Сайт компании</a>`].filter(Boolean).join('');card.innerHTML=`<button class="modal-close" type="button" data-public-seller-close>×</button><header class="public-seller-head">${profile.logoUrl?`<img src="${profile.logoUrl}" alt="">`:`<span>${initial}</span>`}<div><small>${profile.professional?'Компания':'Частный продавец'}${profile.verified?' · ✓ проверена':''}</small><h2>${escapeHtml(profile.name)}</h2>${profile.workingHours?`<p>${escapeHtml(profile.workingHours)}</p>`:''}</div></header><div class="public-seller-stats"><div><b>${profile.rating||'—'} ★</b><span>${profile.reviewCount||0} отзывов</span></div><div><b>${profile.listingCount||0}</b><span>объявлений</span></div><div><b>${profile.dealCount||0}</b><span>сделок</span></div></div>${profile.description?`<p class="public-seller-description">${escapeHtml(profile.description)}</p>`:''}${contact?`<div class="public-seller-contacts">${contact}</div>`:''}${(profile.branches||[]).length?`<section><h3>Адреса и площадки</h3>${profile.branches.map(x=>`<article><b>${escapeHtml(x.name)}</b><span>${escapeHtml(x.city)} · ${escapeHtml(x.address)}</span></article>`).join('')}</section>`:''}<section><h3>Отзывы покупателей</h3>${(profile.reviews||[]).map(x=>`<article class="seller-review"><b>${'★'.repeat(x.rating)}${'☆'.repeat(5-x.rating)}</b><p>${escapeHtml(x.comment)}</p><small>${new Date(x.createdAt).toLocaleDateString('ru-RU')}</small></article>`).join('')||'<p class="seller-profile-empty">Отзывов после подтверждённых сделок пока нет.</p>'}<button class="seller-review-add" type="button" data-add-seller-review>Оставить отзыв о сделке</button></section><button class="primary-btn" type="button" data-public-seller-message>Написать продавцу</button>`;card.querySelector('[data-public-seller-close]').onclick=()=>modal.classList.remove('open');card.querySelector('[data-public-seller-message]').onclick=()=>{modal.classList.remove('open');document.querySelector('#sendMessage')?.click()};card.querySelector('[data-add-seller-review]').onclick=async()=>{if(!window.vklucheAuth?.require(()=>link.click()))return;const rating=+prompt('Оценка от 1 до 5','5'),comment=prompt('Кратко расскажите о сделке','');if(!rating||!comment)return;const{error}=await client.rpc('submit_seller_review',{p_listing_id:car.listingId,p_rating:rating,p_comment:comment});if(error)return window.toast?.(error.message);window.toast?.('Отзыв опубликован');modal.classList.remove('open');link.click()}} ,true);
  function escapeHtml(value){const div=document.createElement('div');div.textContent=value??'';return div.innerHTML}
})();
